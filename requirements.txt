# Runtime dependencies for Python scripts in src/
torch>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0
oss2>=2.18.0        # Alibaba Cloud OSS SDK (needed by oss_roundtrip_verify.py)

# C++ extension build dependencies (optional, for rebuilding CodeTensors/cpp)
# cmake >= 3.26
# pybind11 (bundled in CodeTensors/cpp/tools/pybind11)
# googletest (bundled in CodeTensors/cpp/tools/googletest)
# System libs: libzstd-dev, liblz4-dev, libsnappy-dev, zlib1g-dev
